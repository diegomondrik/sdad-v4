# MCP config — contains tokens, never commit
mcp.json

# SDAD runtime state (hook artifacts: compact anchor snapshot, autocommit hold sentinel)
# .sdad/eval/ is the exception: the methodology golden dataset is versioned (v5 I3)
.sdad/*
!.sdad/eval/

# Windows
Thumbs.db
Desktop.ini

# Editor
.vscode/
.idea/
